defmodule RomanNumeralsElixir do

  def convert_number(number) do
    "I"
  end

  # def` convert_number(number) do
  #   lookup_number(number, [["10","X"], ["5" "V", 1: "I"], [])
  # end

  # def lookup_number(number, [current_lookup | remaining_lookup], acc) do
  #   if number == current_numeral do
  #     roman_numeral = Keyword.get_values current_lookup
  #     lookup_number(number = number - , roman_numeral ++ acc
  #   else
  #     "V"
  #   end
  # end

end
